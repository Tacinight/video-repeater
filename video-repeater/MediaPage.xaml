<Page
    
    x:Class="VideoRepeater.MediaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideoRepeater"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:VideoRepeater.Model"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal">
        <GridView Name="MediaGridView"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemsSource="{x:Bind MediaFiles}"
                  ItemClick="MediaGridView_ItemClick"
                  Width="600"
                  
                  >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:MediaFile">
                    <StackPanel>
                        <Image Source="/Assets/pic.png" Width="160" Height="160"/>
                        <Button Content="{x:Bind Name}"/>
                    </StackPanel>
                    
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Button Content="Open" Click="openBtn_Click"  FontSize="26"/>
        <Button Content="OpenFolder" 
                FontSize="26"
                Click="Button_Click"
                Margin="20,0,0,0"/>
    </StackPanel>

</Page>
